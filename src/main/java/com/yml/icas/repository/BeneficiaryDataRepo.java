package com.yml.icas.repository;

import com.yml.icas.model.BeneficiaryData;
import org.springframework.data.jpa.repository.JpaRepository;

public interface BeneficiaryDataRepo extends JpaRepository<BeneficiaryData, Integer> {
}
