server:
  port: 9000
  error:
    include-message: always
    include-binding-errors: always
    include-stacktrace: always

spring:
  application:
    name: member
  datasource:
    username: postgres
    url: jdbc:postgresql://localhost:5432/insurance
    password: abcd

  jpa:
    show-sql: false
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: lakshakumara@gmail.com
    password: yMl@12